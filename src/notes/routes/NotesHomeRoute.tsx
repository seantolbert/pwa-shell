import { NotesShell } from '../components';

/**
 * Placeholder route-level component for /notes.
 * Eventually hosts folder navigation, smart search, and the editor surface.
 */
export const NotesHomeRoute = () => {
  return <NotesShell>NotesHomeRoute placeholder</NotesShell>;
};
